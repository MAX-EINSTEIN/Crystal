#ifndef ITERATOR_H
#define ITEREATOR_H

// For now the iterators are implemented inside the 
// data strcutres itself. In near future, the iterators
// will be made more generic so that same algorithms can be
// applied to similar type of structures without implementing
// each algorithm for many times

#endif